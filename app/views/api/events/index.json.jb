@events.map do |event|
  {
    id: event.id,
    name: event.name,
    date: event.date,
    time: event.time,
    meeting_link: event.meeting_link,
    image: event.image,
    user_id: event.user_id,
    book_id: event.book_id,
    book_title: event.book_title,
    book_subtitle: event.book_subtitle,
    book_author: event.book_author,
    book_description: event.book_description,
  
  }
end